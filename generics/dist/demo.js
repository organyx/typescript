"use strict";
function getRandomElement3(list) {
    return list[Math.floor(Math.random() * list.length)];
}
const getRandomElement2 = (list) => {
    return list[Math.floor(Math.random() * list.length)];
};
